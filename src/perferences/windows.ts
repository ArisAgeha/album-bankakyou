import { perferences } from '@/typings/preferences';

export const mainPreferences: perferences = {
  windows: {
    fullscreen: 'ph.windows.fullscreen',
    width: 'ph.windows.width',
    height: 'ph.windows.hegiht'
  }
};

export const processConfig = {
    LOCALE_LANGUAGE: 'process.locale.language'
};

export const windowsConfig = {
    SIZE_FULLSCREEN: 'windows.size.fullscreen',
    SIZE_WIDTH: 'windows.size.width',
    SIZE_HEIGHT: 'windows.size.height'
};

export const workbenchConfig = {
    FILEBAR_WIDTH: 'workbench.fileBar.width',
    FILEBAR_SHOW: 'workbench.fileBar.show',
    MANAGEBAR_HEIGHT: 'workbench.manageBar.height',
    MANAGEBAR_SHOW: 'workbench.manageBar.show'
};